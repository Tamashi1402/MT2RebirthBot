# Delve ESP / YOLO / torch was removed. Delve mode does not use AI.
# These stubs exist so any leftover import does not crash the bot.


def boss_missing_for(seconds=None) -> bool:
    return False


def preload_delve_esp():
    return None


def start_delve_esp():
    return None


def stop_delve_esp():
    return None
